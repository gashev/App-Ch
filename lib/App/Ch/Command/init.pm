# ABSTRACT: create a new repository
package App::Ch::Command::init;

use strict;
use warnings;

use base 'App::Ch::Command';

our $VERSION = '0.1';

sub execute {
    my ( $self, $opt, $args ) = @_;
}

1;
